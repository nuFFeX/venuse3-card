# Localize (Referenz)

Die Karte lädt **keine** Dateien aus diesem Ordner mehr. Alle Texte stehen im Objekt `languages` in `venuse3-card.js` (wie beim gebündelten b2500d-Release).

`en.js` und `de.js` bleiben als lesbare Kopie; bei Änderungen bitte `venuse3-card.js` entsprechend anpassen.
